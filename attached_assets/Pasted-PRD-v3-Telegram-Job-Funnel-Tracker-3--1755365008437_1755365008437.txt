PRD v3 — Telegram-бот «Job Funnel Tracker»

Итерация 3: Авто-предложение формы рефлексии после изменений счётчиков (обновлено)

Коротко

Бот отслеживает только изменения указанных счётчиков за выбранную неделю и канал. Если любой из них увеличился на +≥1, бот спрашивает, хочет ли пользователь заполнить форму рефлексии. Изменение CVR само по себе не является триггером. Каждая единица прироста при согласии пользователя превращается в отдельную форму в очереди. Все ответы видны только владельцу (user_id).

Триггеры формы (обновлено)

Единственный триггер — прирост на +≥1 по одному из полей ввода ниже:

«Теперь введите количество ответов от компаний (Responses):»

«Теперь введите количество первичных звонков/скринингов (Screenings):»

«Теперь введите количество основных интервью (Onsites)
(Если у одной компании несколько интервью — считайте как 1):»

«Теперь введите количество полученных офферов (Offers):»

«Наконец, введите количество отказов (Rejections):»

Не триггер: любые пересчёты CVR (даже если они изменились).

Примечание: для пассивной воронки поля Views/Inbounds не являются триггером.

Поведение после срабатывания триггера (обновлено)

Показываем пользователю вопрос: «Заполнить форму рефлексии сейчас?»
Кнопки: Да · Нет.

Если Да:

вычисляем Δ = new_value - old_value для соответствующего счётчика;

создаём Δ элементов в очереди форм в контексте week_start + channel + funnel_type;

открываем первую форму.

Если Нет: ничего не создаём; форму можно открыть позднее вручную через /log_event.

Содержимое формы (не изменялось)

Тип этапа (кнопки + подсказки связанных H):

✉️ Ответ (CVR1 = Ответы/Подачи) → H1, H2

📞 Скрининг (CVR2 = Скрининги/Ответы) → H2, H3

🧑‍💼 Онсайт (CVR3 = Онсайты/Скрининги) → H3, H4

🏁 Оффер (CVR4 = Офферы/Онсайты) → H5

❌ Отказ без интервью (не влияет на CVR)

❌❌ Отказ после интервью (не влияет на CVR) → указать: после Скрининга или Онсайта

Как прошло (оценка) — 1…5

Сильные стороны — опционально

Слабые стороны / пробелы — опционально

Самочувствие и мотивация — 1…5

Причина отказа (если выбран «Отказ…») — мультивыбор:
Нет нужного навыка · Нет культурного совпадения · Локация/виза · Язык · Зарплата/бюджет · Нет доменного опыта · Сроки/доступность · Другое (+текст)

Шапка формы: Неделя · Канал · Тип воронки.

Правила и крайние случаи (уточнено)

Очередь форм: создаётся только после согласия пользователя. Показываем по одной; есть Пропустить.

Откат значений: если после создания очереди пользователь уменьшил счётчик, помечаем последние k pending-форм этого контекста/этапа как void. Уже done не трогаем; минимально допустимое значение счётчика = число done.

Тайм-аут: незавершённая форма остаётся в очереди; 1 напоминание/день.

Конкуренция: FSM per user; один процесс.

UX / Команды (уточнено)

После ввода данных и фиксации прироста: карточка с вопросом «Заполнить форму рефлексии сейчас?» → Да/Нет.

Кнопки контекста (если очередь уже есть): ▶ Заполнить, ⏭ Пропустить, 🗑 Отменить.

Ручные команды:

/log_event — открыть форму без привязки к триггеру; сначала выбрать Неделю и Канал.

/pending_forms — показать и начать заполнение очереди (если есть).

/last_events — последние N записей (монотаблица).

Остальная спецификация (без изменений)

H↔CVR справка, схема БД, технические требования, критерии приёмки, интеграция с v1/v2 — остаются как в предыдущей версии PRD v3.

Напоминание: в v1 данные записываются суммированием (additive upsert) по ключу user_id + funnel_type + channel + week_start.